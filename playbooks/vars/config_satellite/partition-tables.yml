---

- name: RHEL8 Kickstart Partition Table
  file: files/satellite/rhel8-ptable.erb
  os_family: Redhat
  organizations:
    - name: "{{ satellite_default_organization }}"
  locations:
    - name: "{{ satellite_default_location }}"
