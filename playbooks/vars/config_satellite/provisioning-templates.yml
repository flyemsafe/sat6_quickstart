---

- name: Provision RHEL
  file: files/satellite/provision-rhel.erb
  organizations:
    - "{{ satellite_org }}"
  locations:
    - "{{ satellite_location }}"
- name: register-rhel
  file: files/satellite/register-rhel.erb
  organizations:
    - "{{ satellite_org }}"
  locations:
    - "{{ satellite_location }}"
